apiVersion: route.openshift.io/v1
kind: Route

metadata:
  name: {{ include "helloworld-chart.fullname" . }}-route

spec:
  to:
    kind: Service
    name: {{ include "helloworld-chart.fullname" . }}

  port:
    targetPort: http